{
  "name": "originmind-public-preview",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "scripts": {
    "build:frontend": "node scripts/build-frontend.mjs",
    "check:frontend": "node --check frontend/app.js && node --check frontend/message-actions.js && node --check scripts/build-frontend.mjs && node scripts/build-frontend.mjs --check",
    "test": "node --test",
    "check": "node --check src/index.mjs && npm run check:frontend && node --test",
    "check:wrangler": "wrangler deploy --dry-run --strict --config wrangler.example.jsonc --outdir .wrangler-build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "katex": "0.18.7",
    "wrangler": "4.131.0"
  }
}
